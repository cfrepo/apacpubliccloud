projection;
strict( 2 );
use draft;

define behavior for ZC_AONLINESHOP_002 alias OnlineShop
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
  use action upload_file as upload;
  use action createPurchaseRequisitionItem;
}